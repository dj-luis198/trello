{
  "name": "trello",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress open --e2e -b electron",
    "post-test": "npx cypress run --spec cypress/e2e/apiTrello/postCards.cy.js",
    "put-delete-test": "npx cypress run --spec cypress/e2e/apiTrello/putDeleteCards.cy.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.0.0",
    "cypress-dotenv": "^2.0.0",
    "dotenv": "^16.0.3"
  },
  "dependencies": {
    "cypress-plugin-api": "^2.11.0"
  }
}
